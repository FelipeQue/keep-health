import { AgeFromDatePipe } from './age-from-date.pipe';

describe('AgeFromDatePipe', () => {
  it('create an instance', () => {
    const pipe = new AgeFromDatePipe();
    expect(pipe).toBeTruthy();
  });
});
