import { CmToMetersPipe } from './cm-to-meters.pipe';

describe('CmToMetersPipe', () => {
  it('create an instance', () => {
    const pipe = new CmToMetersPipe();
    expect(pipe).toBeTruthy();
  });
});
